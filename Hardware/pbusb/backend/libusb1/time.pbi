; From <bits/time.h>
; struct timeval
;   {
;     __time_t tv_sec;		/* Seconds.  */
;     __suseconds_t tv_usec;	/* Microseconds.  */
;   };

Structure timeval
	tv_sec.q
	tv_usec.q
EndStructure
; IDE Options = PureBasic 5.21 LTS (Linux - x64)
; CursorPosition = 9
; Folding = -
; EnableXP